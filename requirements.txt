# Apple Silicon (Metal GPU via MLX). Markers, not comments: the MLX imports are
# lazy (server/asr.py, server/translate.py), so a Linux box or a container can
# install this same file and run the OpenAI engine without them.
mlx>=0.29; sys_platform == "darwin" and platform_machine == "arm64"
mlx-whisper>=0.4.3; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.28; sys_platform == "darwin" and platform_machine == "arm64"

fastapi>=0.115
uvicorn[standard]>=0.30
numpy>=1.26
httpx>=0.27          # OpenAI speech-to-text option

# Slides are rendered in the browser (web/vendor/pdf.js), so the server needs
# no PDF library at all.
